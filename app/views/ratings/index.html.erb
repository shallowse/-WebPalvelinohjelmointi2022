<h2>List of ratings</h2>

<h4><%= pluralize(@ratings.count, 'rating') %></h4>
<ul>
  <% @ratings.each do |rating| %>
    <li>
      <%= render rating %> 
      <%= link_to rating.user.username, user_path(rating.user.id) %>
    </li>
  <% end %>
</ul>

<%= link_to "Add new rating", new_rating_path, class: 'btn btn-primary' %>